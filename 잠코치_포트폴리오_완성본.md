# 🌙 잠코치 (SleepCoach) — 개발 포트폴리오

---

## 📌 한눈에 보기

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 잠코치 (SleepCoach) |
| 한 줄 소개 | 청소년 수면·스마트폰 습관을 기록하고 AI가 분석해주는 웹앱 |
| 배포 링크 | [https://ora1012.github.io/sleepcoach/](https://ora1012.github.io/sleepcoach/) |
| GitHub | [github.com/ora1012/sleepcoach](https://github.com/ora1012/sleepcoach) |
| 개발 기간 | 2026.06 ~ (진행 중) |
| 사용 기술 | HTML/CSS/JS · Supabase(Auth+DB) · Cloudflare Workers · Gemini API |
| 개발 방식 | 기획·설계 직접 / 구현 AI(Antigravity) 활용 / 결과 이해·검수 |

---

## ① 왜 만들었나

**계기**
밤에 스마트폰을 늦게까지 보다 잠이 부족한 채로 등교하는 날이 반복됐다. 피곤한 게 익숙해질수록 "이 정도면 괜찮겠지"라고 넘기게 되었고, 내 수면이 정말 부족한 건지 객관적으로 확인할 방법이 없었다.

**조사**
조사해 보니 나만의 문제가 아니었다. 한국 청소년의 평균 수면 시간은 약 5.8시간으로 OECD 국가 중 최하위이고, 중학생 52%·고등학생 90%가 권장 시간(8~10시간)에 못 미친다. **그런데 정작 잠이 부족한 학생 중 35%만 스스로 '부족하다'고 인식**한다. 한편 청소년 스마트폰 과의존 위험군은 43.0%로 유일하게 증가 추세이고, 폰 사용 습관과 수면의 질은 서로 연결되어 있다.

**제작**
"객관적으로 보여주는 도구가 없다면 내가 만들자." 매일 30초 만에 수면·폰 사용을 기록하면 AI가 내가 몰랐던 패턴을 짚어주고, 오늘 바꿀 수 있는 작은 습관 하나를 추천하는 웹앱 — **잠코치**를 만들었다.

---

## ② 어떻게 기획했나

- **타깃 분석 및 니즈 파악**: 스마트폰을 늦게까지 보다 수면이 부족한 중·고등학생을 타깃으로 설정. 입력이 조금만 귀찮아도 이탈한다는 특징을 반영해 하루 30초면 기록이 끝나는 간편한 UI를 기획했다.
- **핵심 목표 설정**: 사용자가 자기 수면·폰 사용 패턴을 객관적으로 인식하고, 매일 작은 습관 하나를 바꾸도록 돕는 것을 목표로 삼았다. (성공 기준: 3일 연속 기록, AI 인사이트 제공, 실행 가능한 미션 추천)
- **분석 알고리즘 직접 설계**: 단순 평균 통계가 아닌, 4가지 핵심 패턴 분석 규칙(수면 부족량, 폰 영향, 취침 불규칙, 낮 졸림 빈도)을 직접 수학적으로 설계하고 엣지 케이스까지 고려했다.
- **미션 매핑 설계**: 발견된 패턴에 따라 "30분만 일찍 불 끄기", "11시 전에 폰 충전기에 꽂아두기" 등 5종의 즉각적이고 실천 가능한 오늘의 미션을 매핑했다.
- **기능 범위(MVP) 정의**: 한 번에 너무 많은 기능을 욕심내지 않도록 MVP(데일리 기록, AI 패턴 분석, 오늘의 미션)와 2순위 기능(수면 점수, 수면 달력, 대시보드 등)을 명확히 구분하여 개발 로드맵을 세웠다.

---

## ③ 어떻게 만들었나

> 개발 과정을 순서대로 적었다. 잠코치가 어떻게 자라났는지가 보일 것이다.

### 1단계. 화면 만들기 — 기록·분석·미션 3개 화면 배치 (6월)

가장 먼저 만든 건 **입력 화면**이다. PRD에 "하루 30초 기록"이 핵심이라 적었으니, 화면부터 간결하게 잡아야 했다. 수면 시각 2개, 폰 사용 슬라이더, 이모지 컨디션, 낮 졸림 토글 — 5개 항목만 두고, 저장 누르면 분석 화면으로 넘어가게 했다. 분석 화면에는 주간 요약 카드·패턴 카드·AI 한마디를, 미션 화면에는 오늘의 미션 카드를 배치했다.

이 순서인 이유: **입력이 존재해야 분석이 되고, 분석이 있어야 미션이 나온다.** 데이터 흐름 자체가 기록→분석→미션이라 화면도 이 순서대로 만들었다.

> 기획과 분석 규칙(수면 부족량, 폰 영향, 취침 불규칙, 낮 졸림 빈도 4가지)은 **직접 설계**했고, 구현은 AI(Antigravity)에 맡겼다. 단, AI가 만든 코드를 그냥 수락한 게 아니라 — 수면 시간 자동 계산(자정 넘김 처리)이 정확한지, 표준편차 공식이 맞는지, 중앙값 분류가 올바른지를 하나씩 확인한 뒤 Accept 했다.

### 2단계. 인터넷에 공개하기 — GitHub Pages 배포 (6월)

화면이 돌아가는 걸 확인하고 바로 배포했다. GitHub에 코드를 올리고, GitHub Pages를 켰더니 주소(`https://ora1012.github.io/sleepcoach/`)가 생겼다. 이때 구글 로그인 콜백이 404를 뿜었는데, 배포 URL 경로를 `redirectTo`에 맞추니 해결됐다.

링크를 친구들에게 보냈더니 진짜로 기록을 남기기 시작했다. **내가 만든 걸 다른 사람이 쓰고 있다**는 게 신기하고 좋았다. 하지만 바로 다음 문제를 만났다.

### 3단계. AI 기능 붙이기 — Gemini API 연결, 그리고 키 은닉 (7월 초)

AI 코멘트를 달기 위해 Gemini API를 연결했다. 처음엔 프론트엔드에 API 키를 직접 넣었다. 작동은 했지만, **코드가 공개 저장소에 그대로 올라간다**는 걸 깨달았다. 누구나 내 키를 가져갈 수 있었다.

그래서 **Cloudflare Workers로 서버리스 프록시**를 만들었다. 키는 백엔드 환경 변수에 숨기고, CORS 설정으로 내 GitHub Pages 도메인(`https://ora1012.github.io`)의 요청만 받도록 잠갔다. 프론트엔드에선 키가 완전히 사라졌다.

> "기획과 분석 규칙은 직접 설계했고, 구현은 AI에 맡겼지만 나온 코드를 이해하고 검수했다"가 정직하면서 강한 표현이다. 실제로 그렇게 했으니까.
>
> · 코드를 읽다가 "아 이래서 되는구나" 했던 순간 — Worker의 `env.GEMINI_API_KEY`가 브라우저에 절대 노출되지 않는 구조를 이해했을 때.

### 4단계. 구글 로그인 — "내 계정" 개념이 생긴 순간 (7월)

처음엔 모든 기록이 브라우저 LocalStorage에만 저장됐다. 폰을 바꾸면 기록이 날아간다. **"내 기록은 어디서든 보여야 한다"**는 생각에 로그인 기능을 붙이기로 했다.

Supabase의 구글 OAuth를 연동했다. 여기서 배운 게 있다 — 로그인이 단순히 "로그인 버튼"이 아니라, **사용자(user_id)와 데이터를 묶는 끈**이라는 것. 계정이 있어야 "이 기록이 누구 것인지" 구분할 수 있고, 그래야 다음 단계(DB 저장)가 의미가 있다.

### 5단계. 기록을 DB로 — 폰을 바꿔도 기록이 남게 (7/26)

로그인 기능이 생긴 뒤, 기록 저장을 LocalStorage에서 **Supabase 클라우드 DB(records 테이블)**로 옮겼다.

여기서 직접 이해하고 넘어간 것들:
- **RLS(Row Level Security)**: `auth.uid() = user_id` 조건의 정책 4개(select/insert/update/delete)를 설계했다. select에 "내 것만" 조건(where)을 안 넣어도 DB가 정책으로 걸러주기 때문에, **코드가 실수해도 남의 데이터가 안 샌다**는 걸 확인했다.
- **upsert**: 같은 날 다시 저장하면 새 줄이 안 생기고 덮어쓰는 구조. 테이블의 `unique(user_id, date)` 제약과 한 쌍으로 움직인다는 걸 코드에서 직접 확인했다.

> SQL은 Antigravity가 만들어줬지만 직접 실행할 수는 없었다. 그래서 **Supabase 대시보드 SQL Editor에 붙여넣어 직접 Run 하고, 결과창으로 정책이 제대로 만들어졌는지 검수**했다.

### 6단계. 달력 화면 — 숫자 대신 색으로 한 달을 보여주기 (7/26)

DB가 생기니 "날짜별 기록"을 꺼내올 수 있게 됐다. 그래서 **달력(캘린더) 탭**을 추가했다. 날짜마다 수면 시간에 따라 색이 다르게 칠해진다:
- 기록 없음: 짙은 회색 / 부족(<6h): 연보라 / 보통(6~7.5h): 파랑 / 충분(>7.5h): 진한 파랑

한 달을 한눈에 보면 "이번 주 연보라가 많네" 하고 직감적으로 알 수 있다. 숫자 나열보다 **색 한 칸이 더 세게 말해준다**는 걸 만들면서 느꼈다.

---

### 최종 구조

개발이 끝난 뒤 잠코치의 구조는 이렇게 됐다. 서버를 따로 띄우지 않고 클라우드 서비스 4개를 조합해서 **상용 앱 수준의 아키텍처**를 만들었다.

```
[브라우저 · GitHub Pages — 정적 호스팅]
 ├─ 기록/분석/미션/달력 UI (HTML·CSS·Vanilla JS)
 ├─ 로그인 → Supabase Auth (구글 OAuth)
 ├─ 기록 저장 → Supabase DB (records 테이블 + RLS 정책 4개)
 └─ AI 코멘트 → Cloudflare Workers (API 키 은닉) → Gemini 2.5 Flash
```

---

## ④ 막혔던 순간들

### 배포했는데 404

GitHub Pages로 배포를 끝내고 친구들에게 링크를 보냈다. "구글 로그인 한번 해봐." 친구가 로그인 버튼을 눌렀고, 구글 인증 화면까지는 잘 넘어갔다. 그런데 인증이 끝나고 돌아오는 콜백 페이지에서 404가 떴다. 친구한테 "잠깐만" 하고 바로 확인에 들어갔다.

처음엔 GitHub Pages 설정이 잘못된 줄 알았다. Settings 페이지에 들어가서 배포 브랜치가 맞는지, 경로가 `/`로 되어 있는지 확인했다. 문제없었다. 그다음엔 Supabase 대시보드의 Redirect URL 설정을 의심했다. 혹시 등록된 URL에 오타가 있거나 빠진 건 아닌가? 확인해보니 거기도 정확히 맞았다. 두 군데 다 맞는데 404가 뜨니까 진짜 답답했다.

결국 "설정은 다 맞는데 안 된다면, 실제로 요청이 어디로 날아가는지 직접 봐야 한다"고 생각했다. 브라우저 개발자 도구를 열고 네트워크 탭에서 콜백 요청을 하나씩 따라갔다. 그제야 보였다 — 코드 안의 `redirectTo` 값이 로컬 개발 주소(`http://localhost`)로 되어 있었다. 내 컴퓨터에서 테스트할 때는 잘 됐으니 당연히 의심을 안 했던 것이다. 이 값을 배포 URL(`https://ora1012.github.io/sleepcoach/`)로 바꾸니 바로 해결됐다.

이때 배운 건 **배제법 디버깅**이다. "이것 때문인가?" 확인하고 아니면 지우고, 다음 것을 확인하고 또 아니면 지우고. 설정을 하나씩 제거하면서 범위를 좁혀가는 과정을 처음 몸으로 익혔다. 그리고 **로컬에서 되는 것과 배포 환경에서 되는 것은 다르다**는 걸 뼈저리게 느꼈다.

### API 키를 어디 둘까

Gemini API를 연결하고 AI 코멘트가 달리기 시작했을 때, 기분이 정말 좋았다. "드디어 AI가 붙었다!" 그런데 코드를 GitHub에 푸시하고 나서 소름이 끼쳤다. API 키가 JavaScript 파일에 문자열로 그대로 박혀 있었고, 내 저장소는 공개(public)라 누구든 코드를 열면 키가 보였다.

처음엔 `.gitignore`에 키가 든 파일을 넣으면 되지 않을까 생각했다. GitHub에 안 올라가면 남이 못 보는 거니까. 그런데 한 발짝 더 생각해보니 아니었다. 프론트엔드 코드는 결국 브라우저에 다운로드되어 실행되기 때문에, `.gitignore`로 파일을 숨겨도 빌드 결과물에 키가 포함되는 건 똑같았다. 브라우저 개발자 도구 소스 탭을 열면 누구든 볼 수 있었다.

그다음엔 "환경 변수로 빼면 되지 않나?" 싶었다. 하지만 GitHub Pages는 정적 호스팅이라 서버 사이드 환경 변수라는 개념이 없었다. 결국 **키를 브라우저 밖으로 완전히 빼야 한다** — 즉, 키를 들고 있는 서버가 따로 있어야 한다는 결론에 도달했다.

그래서 Cloudflare Workers로 서버리스 프록시를 만들었다. 구조는 이렇다: 프론트엔드가 Worker 주소로 프롬프트만 보내면, Worker가 자기 환경 변수(`env.GEMINI_API_KEY`)에서 키를 꺼내 Gemini API를 대신 호출하고, 결과만 돌려준다. 거기에 CORS 설정으로 내 GitHub Pages 도메인(`https://ora1012.github.io`)의 요청만 허용하도록 잠갔다. 프론트엔드 코드에서 키가 완전히 사라진 걸 `Ctrl+F`로 확인하고 나서야 안심이 됐다.

"코드에 비밀을 넣으면 안 된다"는 말은 여러 번 들었지만, 직접 키가 공개 저장소에 노출된 걸 보고 나서야 왜 위험한지 실감했다. 그리고 **프론트엔드와 백엔드를 나누는 이유** 중 하나가 바로 이것 — 브라우저에 보여주면 안 되는 것을 숨길 장소가 필요하기 때문 — 이라는 걸 직접 겪으며 배웠다.

### 남의 기록이 섞이면?

로그인을 붙이고 Supabase DB에 기록을 저장하기 시작했다. 잘 돌아가는 걸 보면서 한 가지 걱정이 올라왔다. 여러 사람이 같은 `records` 테이블에 기록을 넣는데, 만약 내 코드에 실수가 있어서 조회할 때 `where user_id = ...` 조건을 빠뜨리면? 다른 사람의 수면 기록, 폰 사용 시간이 내 화면에 그대로 보이는 것이다. 개인 습관 데이터라서 더 찝찝했다.

처음엔 "코드에서 필터만 잘 걸면 되잖아"라고 생각했다. 조회할 때마다 `.eq('user_id', userId)` 조건을 꼼꼼히 넣으면 문제없다고. 그런데 곰곰이 따져보니, 그건 **코드를 믿는 것**이지 **구조를 믿는 것**이 아니었다. 지금은 조건을 잘 넣었어도, 나중에 새 기능을 추가하면서 실수로 한 군데를 빼먹으면? 코드는 언제든 틀릴 수 있다.

그래서 Supabase의 RLS(Row Level Security)를 공부했다. RLS는 DB 테이블 자체에 "이 행은 이 사용자만 접근 가능"이라는 규칙을 거는 기능이다. `auth.uid() = user_id` 조건의 정책 4개(select, insert, update, delete)를 직접 설계해서 SQL로 테이블에 걸었다. Antigravity가 SQL을 만들어줬지만 직접 실행할 수는 없어서, 내가 Supabase 대시보드의 SQL Editor에 붙여넣고 Run을 눌렀다. 결과창에서 정책 4개가 제대로 생성됐는지 하나씩 확인했다.

이렇게 하면 프론트엔드 코드에서 where 조건을 빼먹어도, DB가 정책 단에서 남의 데이터를 차단한다. 실제로 확인해봤다. RLS를 켠 뒤 일부러 조건 없이 전체 조회(`.select('*')`)를 시도했는데, 내 기록만 나왔다. 코드가 구멍이 나도 데이터가 안 새는 걸 눈으로 보고 나니까 비로소 안심이 됐다.

**코드가 실수해도 데이터가 안 새는 구조**를 만드는 게 진짜 보안이라는 걸 이때 배웠다. 코드 레벨의 필터는 1차 방어선이고, DB 정책은 마지막 방어선이다. 두 겹이 있어야 한다.

### AI가 갑자기 멈췄을 때

개발 막바지였다. 코드를 수정하고 배포까지 마무리해야 하는데, AI 에이전트(Antigravity)가 에러를 내면서 갑자기 멈춰버렸다. 처음엔 "일시적인 오류겠지" 하고 다시 실행했다. 같은 에러가 또 났다. 한 번 더 시도했는데 또 같은 에러. 그렇다고 수정한 코드를 배포 안 하고 놔둘 수도 없었다. 사용자들이 쓰고 있는 서비스라 빨리 반영해야 했다.

솔직히 잠깐 막막했다. 그동안 코드를 고치면 Antigravity가 커밋하고 푸시해줬으니까, 내가 직접 배포해 본 적이 없었다. 하지만 생각을 정리해보니 깨달았다 — Antigravity가 해주던 것도 결국은 터미널에서 git 명령어를 치는 것이었다. 에이전트가 실행하는 명령어를 매번 옆에서 봤기 때문에, 무슨 명령어를 쳐야 하는지 알고 있었다.

터미널을 직접 열었다. `git add .`으로 수정된 파일을 스테이징하고, `git commit -m "fix: ..."`로 커밋을 만들고, `git push`로 GitHub에 올렸다. 명령어를 하나씩 확인하며 쳤더니, 배포가 정상적으로 완료됐다. 에이전트 없이도 할 수 있다는 게 확인되니까 오히려 뿌듯했다.

**도구가 해주던 일의 실체를 알고 있으면, 도구가 멈춰도 내가 멈추지 않을 수 있다.** 이게 이 경험에서 얻은 가장 큰 교훈이다. 그 뒤로는 에이전트가 명령어를 실행할 때 그냥 넘기지 않고, "지금 뭘 치는 건지" 의식적으로 읽게 됐다. AI를 쓰되, AI가 하는 일을 이해하고 있어야 한다는 원칙이 이때 생겼다.

---

## ⑤ 배운 것

> 이 프로젝트를 시작할 때의 나와 지금의 나, 뭐가 제일 달라졌어?

### 🔧 기술적으로 배운 것

- **"서버"가 뭔지 몸으로 알게 됐다** — API 키를 JavaScript에 그대로 넣었다가, 브라우저에서 실행되는 코드는 누구나 볼 수 있다는 사실을 깨달았다. Cloudflare Workers로 키를 서버 쪽에 숨기는 구조를 직접 만들면서, 프론트엔드와 백엔드를 왜 나누는지 처음으로 체감했다. 지금은 "보여주면 안 되는 것을 숨길 장소가 백엔드"라고 한 문장으로 설명할 수 있다.
- **DB는 "코드가 실수해도 데이터가 안 새는 구조"를 만드는 곳** — LocalStorage로 충분하다고 생각했지만, 사용자가 늘면서 "남의 기록이 섞이면?"이라는 질문이 떠올랐다. Supabase RLS를 공부해 `auth.uid() = user_id` 정책을 테이블에 걸었더니, 코드에서 where 조건을 빠뜨려도 DB가 정책 단에서 걸러줬다. 보안은 코드 한 줄이 아니라 구조로 잡는 것이라는 걸 이때 배웠다.
- **수학 공식을 코드로 바꾸는 감각** — 분석 알고리즘을 직접 설계하면서, 평균·표준편차·중앙값 같은 수학 개념이 "데이터를 의미로 바꾸는 도구"라는 걸 실감했다. 특히 폰 사용 시간의 중앙값을 기준으로 컨디션을 비교하는 규칙을 만들 때, 극단값에 흔들리지 않으려면 왜 평균 대신 중앙값을 써야 하는지 직접 데이터를 넣어보며 이해했다.
- **"로컬에서 되는 것 ≠ 배포에서 되는 것"** — 배포 후 구글 로그인 콜백이 404를 뿜었다. 설정을 하나씩 배제하며 범위를 좁힌 끝에, `redirectTo`가 `localhost`로 고정되어 있었던 게 원인이었다. 이후로는 배포할 때 "환경이 바뀌면 뭐가 달라지는가"를 가장 먼저 점검하는 습관이 생겼다.

### 💪 태도로 배운 것

- **막혔을 때 멈추지 않는 법을 익혔다** — API 키 노출 문제를 처음 만났을 때는 "내 수준에서 풀 수 있는 건가" 싶었다. 하지만 `.gitignore` → 환경 변수 → 서버리스 프록시 순서로 하나씩 시도하고, 안 되는 이유를 하나씩 이해해 가면서 결국 해결했다. 한 번에 끝내려다 막히는 게 아니라, 모르는 걸 찾아보고 물어보는 과정 자체가 문제 해결이라는 걸 배웠다.
- **"안 된다"가 아니라 "왜 안 되는지"를 먼저 봤다** — 배포 404, RLS 정책 오류, AI 에이전트 다운까지, 막히는 순간이 여러 번 있었다. 그때마다 원인일 수 있는 것들을 하나씩 확인하고 아니면 지워나가는 배제법 디버깅을 반복했다. 이제는 에러 메시지를 보면 겁부터 먹지 않고, 어디서 터졌는지부터 추적하는 게 자연스러워졌다.
- **문제를 남이 정의해 줄 때까지 기다리지 않았다** — "청소년 수면 부족"이라는 문제를 직접 통계를 찾아 정의하고, 해결 방법까지 기획했다. 학교 과제처럼 주어진 문제가 아니라 스스로 발견한 문제를 풀어보는 경험은 처음이었는데, 훨씬 깊이 몰입할 수 있었다.

### 🤖 AI 활용에서 배운 것

- AI(Antigravity)를 **"대신 만들어주는 도구"가 아니라 "같이 만드는 도구"**로 활용했다. 기획·분석 규칙 설계는 직접 했고, 구현은 AI에 맡기되 나온 코드를 반드시 이해하고 검수한 뒤 수용했다.
- 처음엔 AI가 만든 코드를 그대로 Accept 하는 게 편했다. 하지만 RLS가 왜 select에 조건 없이도 내 것만 돌려주는지, upsert가 unique 제약과 어떻게 짝을 이루는지를 내가 설명할 수 없으면 "내 프로젝트"라고 말할 자격이 없다고 느꼈다. 그래서 매번 코드를 읽고, 왜 이렇게 작동하는지 납득한 뒤에만 받아들이는 원칙을 세웠다.
- AI 에이전트가 갑자기 멈춘 적이 있다. 당황했지만, 에이전트가 터미널에서 치던 `git add . → git commit → git push` 명령어를 내가 직접 쳤더니 배포가 됐다. **도구가 해주던 일의 실체를 알고 있으면, 도구가 멈춰도 내가 멈추지 않는다.** 이게 AI를 쓰면서 얻은 가장 큰 교훈이다.

---

## ⑥ 넣고 싶었지만 미룬 것

가장 만들고 싶었던 기능은 **30일 수면 보고서**였다. 한 달간 쌓인 데이터를 AI가 종합 분석해서 "이번 달 평균 취침 시각이 30분 앞당겨졌어요", "폰 사용을 줄인 주에 컨디션이 뚜렷하게 올랐어요" 같은 장기 변화를 한 눈에 보여주는 리포트를 구상했다. 매일의 짧은 분석과 달리, 한 달 단위로 **자기 습관이 실제로 바뀌고 있는지** 확인할 수 있으면 동기부여가 훨씬 강해질 거라고 생각했다.

하지만 이 기능을 제대로 만들려면 30일 분량의 데이터 전처리, 장기 추세 분석 알고리즘, 보고서 UI 설계까지 필요했다. 기존 MVP(데일리 기록·패턴 분석·오늘의 미션)도 아직 다듬을 부분이 남아 있는 상태에서 욕심을 내면 **둘 다 미완성**이 될 게 뻔했다. 그래서 "지금은 MVP를 확실히 끝내자"고 판단하고 미뤘다.

이 경험으로 **기능을 빼는 것도 기획**이라는 걸 배웠다. 넣고 싶은 기능을 참는 게 아니라, 지금 완성할 수 있는 범위를 정하고 그 안에서 최선을 만드는 게 더 중요하다는 걸 알게 됐다.

---

## ⑦ 앞으로 하고 싶은 것

잠코치는 **끝난 프로젝트가 아니라 계속하는 프로젝트**다. MVP를 완성하고 실사용자가 생긴 지금, 다음으로 만들고 싶은 것들이 구체적으로 보인다.

### 📊 대시보드 — 숫자를 그림으로 보여주기
지금 분석 화면은 텍스트 카드 위주다. 여기에 **주간 수면 시간 추이 그래프**, **폰 사용 시간 ↔ 컨디션 상관관계 산점도**, **미션 완료율 도넛 차트**를 넣고 싶다. 매일의 기록이 선으로 연결되면 "이번 주 수면 시간이 점점 늘고 있네" 같은 변화가 한눈에 보일 것이다. Chart.js 같은 라이브러리를 써서 인터랙티브하게 만들 계획이다.

### 🔍 분석 규칙 더 다듬기
현재 4가지 분석 규칙은 잘 작동하지만, 사용자 피드백을 받아보니 더 세밀하게 다듬을 수 있는 부분이 보인다. 예를 들어 **취침 시각이 점점 앞당겨지고 있을 때 그걸 알려주는 개선 트렌드 감지**, **주말과 주중의 수면 패턴 차이(소셜 제트래그) 분석**, **7일이 아닌 30일 장기 추세 리포트** 등을 추가하고 싶다.

### 📱 앱스토어 출시 알아보기
지금은 웹 브라우저에서만 쓸 수 있다. PWA(Progressive Web App)로 전환하면 홈 화면에 앱처럼 추가할 수 있고, 오프라인에서도 기록이 가능해진다. 더 나아가 React Native로 래핑하면 실제 앱스토어에 올릴 수도 있다. "내가 만든 앱이 스토어에 올라간다"는 건 아직 상상만 해도 설레는 목표다.

---

*연계 자료(PDF): 면접 스토리라인 워크시트 / SQL 구문 정리 / 각 작업기록*
*출처: 여성가족부 2025 청소년 미디어 이용습관 진단조사 / 과기정통부 2025 스마트폰 과의존 실태조사 등*
