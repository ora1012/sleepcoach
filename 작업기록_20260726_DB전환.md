# 📅 2026-07-26 작업 기록 · 기록을 DB로 옮기기 (v2 2단계 완료)

| 항목 | 내용 |
|---|---|
| 프로젝트 | 잠코치 (SleepCoach) · 장승준 |
| 배포 링크 | https://ora1012.github.io/sleepcoach/ |
| 오늘의 성과 | 기록을 클라우드 DB로 전환 + 날짜 선택 기능 추가 |
| 진행 방식 | Antigravity 바이브 코딩 (프롬프트 3개 + 대시보드 실행) |

---

## ✅ 오늘 한 것

### 1. Supabase DB 만들기 (프롬프트 ①)
- `records` 테이블 생성 — 기존 LocalStorage 구조 + **user_id**(기록 주인 도장) 추가
- 컬럼: id, user_id, date, sleep_time, wake_time, sleep_hours, phone_minutes, condition, day_sleepy, created_at
- `unique(user_id, date)` — 같은 사람이 같은 날 재저장 시 덮어쓰기 준비
- **RLS 정책 4개** 생성 (select/insert/update/delete), 조건은 전부 `auth.uid() = user_id`
- Antigravity가 SQL을 만들어주고, Supabase 대시보드 SQL Editor에서 직접 Run
- ✅ 검수 통과: 정책 4개 확인, INSERT만 with_check / 나머지는 using

### 2. 코드 전환 (프롬프트 ②)
- script.js의 저장·불러오기를 LocalStorage → Supabase로 전환 (+83 -5)
- 로그인 상태면 DB(upsert/select), 비로그인이면 기존 LocalStorage 유지 (앱이 안 죽게)
- 컬럼명 카멜(JS)↔스네이크(DB) 변환 처리
- 로그인 직후 로컬 기록을 DB로 옮기는 마이그레이션(confirm) 추가
- 분석(Analyzer)·미션 로직은 손대지 않음

### 3. 코드 이해하기 (오늘 공부의 핵심 ★)
Accept 전에 두 곳을 직접 뜯어봄:
- **RLS**: select에 "내 것만" 조건(where user_id)이 없는데도 내 기록만 온다 → DB가 정책으로 걸러주기 때문. 코드가 실수해도 남의 데이터가 안 샌다.
- **upsert**: insert가 아니라 upsert + onConflict('user_id,date') → 같은 날 다시 저장하면 새 줄이 안 생기고 덮어써짐. 테이블의 unique 제약과 한 쌍.

### 4. 날짜 선택 기능 추가 (보너스)
- 좌측 상단 날짜를 클릭하면 달력이 열려 과거 날짜 선택 가능 → 밀린 기록 몰아서 입력
- 미래 날짜는 선택 불가 (오늘까지만)
- 이미 기록한 날을 고르면 그 값이 입력칸에 불러와짐 (수정 편의)
- 저장은 기존 upsert 그대로 → 그 날짜 기록이 있으면 덮어쓰기
- 💡 이 기능이 지금 쉬워진 이유: 날짜별로 정리해 저장하는 DB가 생겼기 때문 (오늘 만든 것의 가치 체감)

---

## 🔧 오늘 배운 개념

| 개념 | 한 줄 정리 |
|---|---|
| 데이터 저장의 3단계 | 변수(칠판) → LocalStorage(그 기기 서랍) → DB(인터넷 금고) |
| 테이블 / 컬럼 / 행 | 엑셀 시트 / 열 제목(타입 있음) / 한 줄 = 기록 1건 |
| SQL | DB에게 시키는 말 — insert·select·update·delete |
| user_id | 기록에 찍는 주인 도장 — 계정과 데이터를 묶는 끈 |
| RLS + 정책 | 행마다 검문하는 자물쇠, auth.uid()=user_id로 "내 것만" |
| upsert | 있으면 덮어쓰고 없으면 넣기 (unique 제약과 짝) |
| 클라우드 DB | 남이 운영하는 DB를 빌려 씀 — Supabase = 서울의 PostgreSQL |

---

## 🧩 막힌 점 & 해결 (면접 스토리 재료 ★)

| 막힌 점 | 어떻게 해결했나 |
|---|---|
| 프로젝트가 아직 켜지는 중(Coming up)에 프롬프트 실행 | Active 될 때까지 기다린 뒤, "현재 상태 확인 후 없는 것만 생성" 프롬프트로 안전하게 재실행 |
| Antigravity가 SQL 실행을 직접 못 함 | 에이전트가 만든 SQL을 대시보드 SQL Editor에 붙여넣어 Run, 결과창으로 검수 |

---

## 🏗️ 현재 잠코치 구조

```
[브라우저 · GitHub Pages]
 ├─ 기록/분석/미션 UI
 ├─ 로그인 → Supabase Auth (구글 OAuth)
 ├─ 기록 저장 → Supabase DB (records + RLS)   ← 오늘 추가!
 └─ AI 코멘트 → Cloudflare Workers → Gemini API
```

---

## 📋 다음 할 일

- [ ] (아직이면) 배포 마무리: "feat: DB 전환 + 날짜 선택" 커밋 & 푸시
- [ ] 폰·노트북 양쪽 로그인 → 같은 기록 뜨는지 최종 확인 + 캡처
- [ ] 3일 이상 두 기기 번갈아 기록 (동기화 체감)
- [ ] 분석 규칙 3(표준편차) 마무리
- [ ] **다음 주: 캘린더 뷰(3단계)** — 날짜별 기록을 달력으로, 수면 시간에 따라 색 농도

---

*연계 문서: 작업기록_20260712 / 잠코치_통합문서 / 강의교재_데이터베이스첫걸음*
