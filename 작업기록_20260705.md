# 🗓️ 작업 기록 (2026-07-05)

## 📌 오늘 완료한 작업 요약

### 1. 2순위 핵심 기능 추가: **수면 점수(Sleep Score)** 구현
- 최근 7일간의 데이터를 종합하여 **100점 만점의 수면 점수**를 산정하는 로직 구현.
- **배점 기준 (세부 로직 직접 설계)**
  - 수면 시간 (40점): 주당 평균 수면 시간을 구간별로 나누어 차등 배점 (예: 8시간 이상 40점)
  - 규칙성 (30점): 취침 시각의 **표준편차(분 단위)**를 계산해 기복이 적을수록 높은 점수 부여
  - 폰 사용 (30점): 평균 자기 전 폰 사용 시간을 구간별로 나누어 배점
- **UI 개선**: 분석 탭의 '이번 주 한눈에' 카드 상단에 점수와 평균 수면시간이 직관적으로 보이도록 Flexbox 레이아웃 및 텍스트 계층(`huge-text`, `medium-text`) 적용.

### 2. MVP 기획서 누락 기능(엣지 케이스) 완벽 보완
- **분석 규칙 3 (취침 시각 불규칙)**: 취침 시각 표준편차가 60분 이상일 때 `"자는 시간이 매일 들쭉날쭉해요"` 패턴 노출 추가.
- **분석 규칙 4 (낮 졸림 빈도)**: 7일 중 낮 졸림(`daySleepy`) 체크 비율이 50% 이상일 때 `"이번 주 절반 이상 낮에 졸렸어요"` 패턴 노출 추가.
- **미션 매핑 및 우선순위 적용**: 추가된 패턴에 대응하는 맞춤형 미션("어제와 같은 시각에 누워보기", "10분 가벼운 산책") 추가 및 미션 노출 우선순위(`sleep_short` > `phone_high` > `sleep_irregular` > `day_sleepy`) 정밀 제어.
- **예외 처리 (F1 폼)**: 수면/기상 시간이 같거나 수면 시간이 14시간을 초과하는 극단적 입력 시 `confirm` 알림을 띄워 사용자 재확인 로직 추가.

### 3. 구조 고도화: **서버리스 프록시(Cloudflare Workers) 전환**
- 기존에 프론트엔드(브라우저)에서 사용자가 API 키를 직접 입력하고 들고 있던 불안정한 구조를 전면 개편.
- **Cloudflare Worker**(`worker.js`)를 생성하여 서버리스 프록시 구축:
  - CORS 설정을 통해 GitHub Pages 배포 도메인(`https://ora1012.github.io`)에서의 요청만 허용.
  - 백엔드(Worker) 환경 변수에 API 키를 은닉하여 보안성 대폭 강화.
- 프론트엔드(`script.js`, `index.html`)에서 불필요해진 API 키 입력 모달, 저장 버튼 등 UI 및 로직 깔끔하게 제거.

### 4. AI 모델 최신화 및 배포
- 프록시 서버 코드에서 호출하는 모델을 기존 지원 종료 예정인 `gemini-1.5-flash`에서 최신 `gemini-2.5-flash`로 무사히 교체.
- GitHub 퍼블릭 레포지토리에 푸시하여 GitHub Pages 자동 배포 완료 및 `README.md`에 배포 링크 정리.

---

## 💡 회고 및 노션 포트폴리오 면접 포인트
- **면접 스토리 1 (능동적 문제 해결)**: "단순히 PRD를 보고 구현하는 데 그치지 않고, `daySleepy` 데이터가 수집만 되고 활용되지 않는 점을 발견하여 기획 의도에 맞는 분석 규칙(4번)을 능동적으로 설계 및 추가했습니다."
- **면접 스토리 2 (보안/아키텍처 개선)**: "프론트엔드 중심의 MVP에서 발생할 수 있는 API 키 유출 위험을 인지하고, Cloudflare Workers를 도입해 백엔드 프록시 서버를 구축함으로써 보안과 구조적 안정성을 한 단계 업그레이드했습니다."
- **면접 스토리 3 (수학적 논리 적용)**: "수면의 '규칙성'을 객관적으로 측정하기 위해 단순히 평균을 내는 것이 아니라, 취침 시각의 분산과 표준편차를 구하는 수학적 알고리즘을 도입해 신뢰도를 높였습니다."
